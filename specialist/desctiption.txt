<?php 
	... - тут год ПХП 
?>
	- открывающие закрывающие дескрипторы

Комментарии
	- // однострочный
 	- /*
 		 многострочный
	  */
; 
	- обязательна

echo "...";
	- '... ', '... ', '... ';
		- передаём несколько параметров
print "...";
	- выводит на экран

strftime('...'); 
	- выводит дату

$var
	- переменная, регистразависемые

unset($var) 
	- удаляем переменную

iconv("windows-1251", "UTF-8", $var);
	- преобразование кодировок 
	- iconv(какую кодировку получаем, в какую кодировку пределываем, переменная);

define("USER_NAME", "Vasa")
const TWO_HUNDRED_TEN
	- константы

setlocale  
	- устанавливает настройки локали

ОШИБКИ
	- E_PARSE		- ошибка парсинга
	  E_ERROR		- фатальная ошибка
	  E_WARNING		- предупреждение
	  E_NOTICE		- пометка
	  E_DEPRECATED	- если метод Диприкейтед

	  Директивы PHP.INI
	  	- display_errors 
	  	- error_reporting

	  error_reporting(E_ALL)
	  	- будем показывать все ошибки

Тип данных 
	- числа
	- $int		= 1234; - целочисленные
	- $float	= 1.23; - не целове

Строка
	- ''		- выводит как есть
	- "" 		- можно использовать спец символы
	- "$name"	- подставляет переменную в "" ковычки